# Function: resolveApiBase()

> **resolveApiBase**(`environment?`, `apiBase?`): `string`

Defined in: src/configuration.ts:11

Resolves and validates the API endpoint for an SDK instance.

## Parameters

### environment?

`"production"` \| `"alpha"`

### apiBase?

`string`

## Returns

`string`
