# Vanilla storefront reference

This intentionally contains no component library or checkout markup. It shows
the SDK calls a website uses before rendering catalog and cart state itself.

Replace the publishable key and connect `CartSnapshot` values to your own UI.
